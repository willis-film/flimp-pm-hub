# macOS Finder metadata
.DS_Store
**/.DS_Store

# Installed by Vercel at build time; never committed
node_modules/

# Local Vercel dev state / env
.vercel
.env
.env.local

# Claude Code per-machine permission grants — local to one checkout, not shared.
# The launch config and dev server alongside it ARE committed; they're useful to
# anyone running the app locally.
.claude/settings.local.json

# Generated kickoff samples from test/kickoff-fixture.mjs
kickoff-sample.pdf
_sample.pdf
