// templates.js — Templates panel.
// Placeholder panel. In the original build this renders an inline
// "Coming soon" stub (emitted by render.js's _stubDefs). No interactive
// handlers yet; this module exists as the mount point for future work.

export {};
